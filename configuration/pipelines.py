# Dictionary with all bash comamnds to run, piepline_management split into live and not live

# ingest latest repository to latest_active_repository in sftp

# for loop that runs commands in dictionary depending on os.getnev("pipeline")