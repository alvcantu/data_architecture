import subprocess
import os

# Set  computer_id as env variables, if computer_id exists already then skip this scripts

if not os.getenv("computer_id"):



# Download and install postgresql

# Setup sftp users (one for latest_Active_reposity (READ ONLY) and the other for develoeprs read and write), sftp private and production folders

# Setup CRON jobs for data pipelines, set export pipeline={corrrect_pipeline}
