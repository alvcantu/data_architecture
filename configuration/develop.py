# ask developer what they want to test
# case of ingestion ask for folder of ingestion files and execute with env variable that makes sure ingest() outputs development schemas
# case of dbt, run dbt build with configuration that ensures target schemas are development