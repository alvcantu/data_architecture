# function that reads sources.yml, output 'schema.table' only if the inputs match something in sources.yml
# depending on whether file is ran with develop.py or by pipelines change output to be schema '{developer_name}_{production_schema}' or '{production_schema}'
